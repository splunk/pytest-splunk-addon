## Total test cases: 4
## CIM Passed: 3
## CIM Failed: 1
## Skipped: 0

# Covers cases for process_name and parent_process_name fields
# Both fields have validity parameter which does not allow
# to contain slashes
# process_name field tests should pass
# parent_process_name field test should fail
[process.sample]
sourcetype = test:process
host_type = plugin
input_type = file_monitor
source = pytest-splunk-addon:file_monitor
sourcetype_to_search = test:process
sample_count = 1
timestamp_type = event

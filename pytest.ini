[pytest]
testpaths = tests
addopts = -v --tb=long -m docker --log-level=INFO -n auto
#    --force-flaky --max-runs=3 --min-passes=1
filterwarnings =
    ignore::DeprecationWarning
#[pytest]

#junit_family = xunit2
